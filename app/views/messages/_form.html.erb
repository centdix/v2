<%= form_with(model: message, local: true) do |form| %>
  <% if message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(message.errors.count, "error") %> prohibited this message from being saved:</h2>

      <ul>
        <% message.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :chat_room_id, value: @chat_room.id %>

  <div class="col-9 offset-md-3 fixed-bottom">
  <div class="input-group input-group-sm m-1">
    <%= form.text_area :content, placeholder: "Type your message...", class: "form-control" %>
    <div class="input-group-append ml-1 col-2">
      <%= form.submit "Send", class: "btn btn-primary btn-block" %>
    </div>
  </div>
  </div>
<% end %>
